
## Использование dcQuery
* Используем _('свойство') вместо $_->getProp('cвойство');
Например `_('name')` - получить имя из объекта флот находящегося в $_
Можно получить сразу несколько свойств
Например `_('x:y')` - получить координаты в формате 500:500
Если в качеств разделителя используется запятая, то функция _() возвращает массив
Например результатом вызова `_('x,y')` - буде массив из 2 элеметов содержащих свойчтва 'x' и 'y' текущего объекта (в $_)